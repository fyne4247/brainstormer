.DS_Store
.claude/settings.local.json
exports/
.brainstormer-hooks-declined

# Each project subfolder is its own git repo — don't track them in the workspace repo
# (list each project folder here as it's created, or use a wildcard pattern)
